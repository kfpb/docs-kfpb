/*!
 * jQuery Bootstrap Wizard Plugin (stub for local compatibility)
 * Prevents "bootstrapWizard is not a function" error
 */
(function($) {
    $.fn.bootstrapWizard = function(options) {
        var defaults = {
            onTabShow: function() {}
        };
        var settings = $.extend(defaults, options);
        return this;
    };
})(jQuery);
